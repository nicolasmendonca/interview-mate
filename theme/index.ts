import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
	colors: {
		bg: '#F7FAFC',
		brand: '#644BFB',
		secondary: '#C3B9FF',
	},
	config: {
		initialColorMode: 'light',
	},
});
